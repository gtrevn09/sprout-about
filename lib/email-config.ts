// ─── EmailJS Setup ────────────────────────────────────────────────────────────
// 1. Create a free account at https://www.emailjs.com (200 emails/month free)
// 2. Add an Email Service  (Gmail, Outlook, etc.) and copy the Service ID below
// 3. Create an Email Template that uses these variables:
//      {{user_name}}   — the recipient's username
//      {{reset_code}}  — the 6-digit reset code
//      {{to_email}}    — the recipient's email address (set as the "To" field)
// 4. Copy your Template ID and Public Key (Account → API Keys) below
// ─────────────────────────────────────────────────────────────────────────────

export const EMAILJS_SERVICE_ID  = 'service_7f1htmg';
export const EMAILJS_TEMPLATE_ID = 'template_kn25057';
export const EMAILJS_PUBLIC_KEY  = 'cnRjhn3xbGTqfI6mV';
